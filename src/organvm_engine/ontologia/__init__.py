"""Ontologia bridge — connects the engine to the structural registry.

This package contains concrete sensor implementations, inference wiring,
governance policy evaluation, state snapshot management, and runbook
generation. All modules degrade gracefully when organvm-ontologia is
not installed.
"""
